#include <stdio.h>
/**
 * main - check the code
 * @argc: check count parameter
 * @argv: check vector parameter
 * Return: returns 0
 */
int main(int argc, char *argv[])
{
(void) argc;
printf("%s\n", argv[0]);
return (0);
}
