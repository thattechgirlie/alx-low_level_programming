#include <stdio.h>
/**
 * main - check code
 * @argc: check count parameter
 * @argv: check vector parameter
 */
int main (int argc, char *argv[])
{
(void) argc;
printf("%s\n", argv[0]);
return (0);
}
